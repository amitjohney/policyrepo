---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kustomize.toolkit.fluxcd.io/ssa: Merge
  name: testdeployment
  labels:
    mytest: test
spec:
  selector:
    matchLabels:
      mytest: test
  replicas: 5
  template:
    metadata:
      labels:
        mytest: test
      name: mynginx
    spec:
      containers:
      - name: nginx
        image: nginx
